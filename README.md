# assert-density-coverage-plugin
Coverage.py reports the lines that were executed. This is an experimental project to make coverage also report the number of asserts executed while running those lines.
